pipeline {
    agent any
    triggers {
        pollSCM('* * * * *')
    }
    stages {
        stage('JAVA') {
            steps {
                sh 'javac Demo.java'
                sh 'java Demo.java'
            }
        }
    }
}
